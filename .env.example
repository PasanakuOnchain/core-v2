# Alchemy — Base mainnet RPC (networks.base / networks.base-fork)
# Create an app at https://dashboard.alchemy.com/ and copy the API key.
# Used in: https://base-mainnet.g.alchemy.com/v2/${ALCHEMY_API_KEY}
ALCHEMY_API_KEY=""

# Blockscout — Base explorer API (contract verify / source lookups)
# Get a key at https://base.blockscout.com/ (Account → API Keys).
# Used as explorer_api_key for networks.base (explorer_uri = https://base.blockscout.com/).
BLOCKSCOUT_API_KEY=""
