[project]
name = "pasanaku"
version = "0.2.0"
description = "Pasanaku is a rotating savings decentralized protocol"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mamushi>=0.1.0",
    "moccasin>=0.4.3",
    "pytest>=9.0.2",
    "ruff>=0.15.9",
    "snekmate>=0.1.2",
    "titanoboa>=0.2.8",
]
